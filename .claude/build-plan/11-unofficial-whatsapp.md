# 11 — Unofficial WhatsApp Module (Baileys QR-bridge) — Build Plan

Scope derived from `screenshots/30-64` (reference app's "Whatsapp" sidebar module) plus the existing inventory in `01-feature-inventory.md` (Whatsapp section) and the v1 commitment already recorded in `05-integrations.md` and `04-data-model.md` (`channels.type = whatsapp_unofficial`). This doc breaks that module into buildable sub-phases under Phase 1's "Messaging" item in `06-roadmap.md`.

## Architecture decision that must be made before coding starts

**Baileys is a Node.js library with no maintained PHP equivalent.** Laravel cannot host the WhatsApp socket connection itself. Recommended shape:

- A small **Node.js "WhatsApp bridge" service** (separate process/deployment from the Laravel API) owns all Baileys sessions — one Baileys socket per connected instance.
- It exposes an internal HTTP API (not public) for Laravel to call: create instance, get QR/pairing code, send message (text/media/group), fetch group participants, disconnect/logout.
- It pushes events **back** to Laravel via signed webhook calls (or a Redis pub/sub queue Laravel already has via Horizon) for: connection status changes, inbound messages, delivery/read receipts.
- Session auth state (Baileys' `useMultiFileAuthState` or a DB-backed equivalent) must survive restarts — store encrypted per-instance session blobs, keyed by `channels.id`, in the bridge service's own storage (or S3/object storage per `02-tech-stack.md`'s file-storage choice), not inside Laravel's DB.
- This mirrors the reference app's UI exactly: `Profile` screen exposing `instance_id` + `access_token` per connected number (screenshot 32/64) is really "credentials Laravel uses to address a specific Baileys session on the bridge."

This is a real infra addition (a second runtime, PHP + Node side by side) — flag it explicitly to whoever owns hosting/deployment before Phase 1a starts; it affects `09-deployment-ops.md`.

**Known gap to verify early:** Baileys has no reliable support for WhatsApp *voice calls* (only call-offer events, not audio). The reference app's "Call Responder" (screenshot 52 — auto-reply to missed/answered/rejected calls) likely just reacts to Baileys' call-offer webhook event and auto-rejects + sends a text reply — it does **not** mean real call handling. Confirm this against current Baileys docs before promising the feature; if unsupported, cut it or scope it down to "auto-reject + auto-text-reply on call-offer event" only.

## Data model additions (extends `04-data-model.md`)

Builds on the existing `channels` / `conversations` / `messages` tables — no need to duplicate those.

- `channels.credentials` (already encrypted json) stores, for `whatsapp_unofficial` rows: `instance_id`, `access_token` (for the module's own REST API, screenshots 38-50), bridge-service instance reference. `channels.status` covers disconnected/connecting/connected/logged_out.
- `whatsapp_groups` — id, channel_id, group_jid, name, participant_count, last_synced_at
- `whatsapp_group_participants` — id, group_id, phone, synced_at (populated on-demand by the Export Participants feature, screenshot 37)
- `whatsapp_campaigns` — id, account_id, channel_id (nullable if "apply to all accounts"), name, contact_group_id (nullable), message_type (text_media/buttons/list/poll/template), body, media_url, spintax_enabled (bool), url_shortener_enabled (bool), emoji_randomizer (bool), warm_up_mode (bool), min_interval_seconds, max_interval_seconds, schedule_window (daytime/nighttime/odd/even/any), recurring (none/daily/weekly/monthly), scheduled_at, status (draft/scheduled/running/completed/failed)
- `whatsapp_campaign_recipients` — id, campaign_id, phone, status (pending/sent/delivered/failed), sent_at, error
- `whatsapp_autoresponders` — id, channel_id (nullable = all accounts), enabled, message_type, body, media_url
- `whatsapp_chatbot_rules` — id, channel_id, enabled, target (all/individual/group), match_type (contains/exact), name, keywords (json array), message_type, body, media_url
- `whatsapp_templates` — id, account_id, name, type (text/text_image/text_video/text_document/text_audio/text_buttons/text_lists/interactive_buttons/text_carousel/poll), content (json — shape depends on type)
- `whatsapp_bot_flows` — id, channel_id, name, flow_definition (json — nodes/edges, same pattern as `ivr_flows.flow_definition` already planned for voice), status (draft/active), source (scratch/template/imported)
- `whatsapp_call_responder_settings` — id, channel_id, auto_reject_enabled, reply_delay_seconds, missed_call_reply, after_call_reply, rejected_call_reply, missed_before_answer_reply
- `whatsapp_short_links` — id, account_id, channel_id, reference_name, phone, message, slug, clicks (for the Link Generator, screenshot 53 — `wa.me` deep links + QR)
- `whatsapp_credit_ledger` — id, account_id, delta, reason (message_sent/bulk_sent/plan_reset/manual_adjustment), balance_after, created_at — the "Available Credits: 98 / Plan Limit: 100" mechanic (feature-inventory line 13). **Decided: ledger, built in 1a.** The fast-read balance itself lives in its own `whatsapp_credit_balances` table (account_id, credits_remaining) rather than a column on `accounts` — see `schema-design-preference` memory (new concerns get their own table, not bolted onto core tables).

Existing `api_tokens` table can likely be reused for the module's public REST API (screenshots 38-50) rather than inventing a new token table — scope the token to `provider: whatsapp_unofficial` and `channel_id`.

## Sub-phases (all still inside roadmap Phase 1's "Messaging" line item)

### 1a — Connection + core send — ✅ built (2026-07-28)
- Bridge hosting decided: own process (systemd/PM2), colocated on the same box as Laravel initially, addressed over `localhost`/internal network only — move to a dedicated VM/container later purely as a config change once WhatsApp instance count makes that worthwhile. Recorded in `09-deployment-ops.md`.
- Backend follows the module-wise convention: controllers under `app/Http/Controllers/Api/Whatsapp/`, routes in their own `routes/api/whatsapp.php` wired into `routes/api.php` via `Route::prefix('whatsapp')->group(...)` — see `backend-modular-controllers` memory.
- Built: `channels`/`conversations`/`messages` tables + models, `ChannelPolicy`, `whatsapp_credit_balances` + `whatsapp_credit_ledger`, `BridgeClient` HTTP service, `ChannelController` (create/QR/pairing-code/status/logout), `MessageController` (single send, decrements credits), `WebhookController` (HMAC-signature-verified inbound events from the bridge), and the `whatsapp-bridge/` Node service itself (Baileys session manager, QR/pairing-code flow, inbound message forwarding).
- Frontend: `views/whatsapp/` (`WhatsappView` + `ChannelsList`/`ConnectAccountDialog`/`SendMessageDialog`), `stores/whatsapp/whatsapp.js`, `router/routes/whatsapp.js`, sidebar entry enabled in `modules.js`.
- Verified via curl smoke test: signed webhook accepted/rejected correctly, connection status + inbound message correctly written to `channels`/`conversations`/`messages`, auth-protected routes reject unauthenticated requests identically to existing modules.
- Not yet exercised with a real phone (needs a live QR scan) — do that manually before considering 1a fully done end-to-end.

### 1a original scope (for reference — see "built" note above)
- Node bridge service skeleton: create instance, QR generation, pairing-code flow, session persistence, disconnect/logout, status webhook back to Laravel
- Laravel: `channels` CRUD for whatsapp_unofficial, `POST /api/whatsapp/instances/{channel}/qr`, connection-status polling or Reverb push to the "Connect WhatsApp" UI (screenshot 64)
- Send Single Message (text/media/template) — screenshot 31 — writes to `messages`/`conversations`
- Inbound message webhook from bridge → `conversations`/`messages`, so Omnichannel inbox has real data from day one
- Profile screen (screenshot 32): show instance_id/access_token, logout
- Contacts (screenshot 63): basic group contact list, enable/disable

### 1b — Bulk + automation + metering — ✅ built (2026-07-28)
- Built: `whatsapp_campaigns`/`whatsapp_campaign_recipients` + `CampaignController` (per-recipient `SendCampaignMessageJob` dispatched with server-enforced cumulative delay — min 3s/max 300s floor+ceiling regardless of client input, doubled further under warm-up mode), `whatsapp_autoresponders` + `AutoresponderController`, `whatsapp_chatbot_rules` + `ChatbotRuleController`, a small `Spintax` parser (`{a|b|c}` random-choice, nested-brace safe), and a `DashboardController` (credits/messages-sent/bulk-delivered/active-rule stat aggregates).
- Chatbot rules and the autoresponder are now wired into `WebhookController::handleInboundMessage` — chatbot keyword matches take priority, autoresponder is the unconditional fallback, both channel-specific and account-wide (`channel_id = null`) rules supported; auto-replies decrement credits and get skipped (not queued/retried) when the balance is 0.
- Frontend: WhatsApp module restructured with a local feature sidebar (Dashboard/Accounts/Bulk messaging/Autoresponder/Chatbot) instead of a flat page, matching the reference app's layout (screenshot 30) — `WhatsappDashboardPanel` (stat cards, message-distribution bars, performance summary), `BulkCampaignsPanel`+`NewCampaignDialog`, `AutoresponderPanel`+`AutoresponderDialog`, `ChatbotPanel`+`ChatbotRuleDialog`.
- Verified via tinker: dashboard aggregate queries run clean, campaign/recipient relations work, spintax renders (including nested groups), chatbot keyword matching (contains/exact) confirmed both ways. Not yet verified with a real live bulk send (would need to actually message real numbers) — the anti-ban interval math and credit decrement path are covered, the live-send path reuses the already-verified `BridgeClient::sendMessage` from 1a.

### 1b original scope (for reference — see "built" note above)
- Bulk messaging / campaigns (screenshot 33) — recipient targeting (contact group), scheduling window, min/max send interval, warm-up mode, recurring schedule — **anti-ban pacing is not optional here**, per `05-integrations.md`: enforce min/max interval server-side regardless of what the UI sends, cap messages/minute per instance
- Spintax message randomization (`{Hi|Hello|Hola}` syntax, screenshot 34) — small parser, no external dependency needed
- URL shortener (feeds `whatsapp_short_links` or a generic shortener if one already exists elsewhere)
- Autoresponder (screenshot 34) and keyword Chatbot (screenshot 34/35) — both are simple rule-match-and-reply; chatbot adds keyword matching (contains/exact) and target scoping (all/individual/group)
- Credit/usage metering: decrement on every successful send, surface on the WhatsApp Analytics dashboard (screenshot 30), gate via `plans.limits` per the existing plan-permission model (feature-inventory line 120)

### 1c — Auxiliary tools
- Templates (screenshot 36) — the 10 message types listed above; likely shared/reused by Bulk messaging, Chatbot, and Bot Builder message nodes rather than duplicated per-feature
- Export Participants (screenshot 37/90) — ✅ built (2026-07-30): groups are discovered *passively*, not via a live "list all my groups" call — matches the reference UI's own instructions (screenshot 90's "1. Send a message to the group you want to export"). `sessionManager.js`'s `messages.upsert` now branches on `@g.us` remoteJid: group traffic fires a lightweight `group.seen` webhook (`WebhookController::handleGroupSeen` upserts into `whatsapp_groups`) instead of going through the normal `message.inbound`/conversation path — fixes a latent bug where group JIDs were being stored as garbage "phone numbers" and could trigger autoresponder/chatbot replies to a non-address. "Download" (screenshot 90) is a single fetch-fresh-then-export action: `GroupController::export` calls `BridgeClient::fetchGroupParticipants` (bridge's on-demand `sock.groupMetadata()`, connected instance required), replaces the group's `whatsapp_group_participants` rows, updates `name`/`participant_count`/`last_synced_at`, then streams the CSV. Frontend: `GroupsPanel.vue` (account select w/ connected-status chip, static "How to use?" card reproducing the 3 screenshot steps, Download disabled when the account isn't connected) + `GroupsList.vue` (data table).

**Fixed after first live test (2026-07-30):** the group-vs-1:1 branch in `messages.upsert` originally sat *after* the `fromMe` filter, so the exact scenario step 1 describes — the connected account sending the test message into the group itself — got silently skipped before the group check ever ran, meaning nothing showed up. The `@g.us` check now runs first, `fromMe` is only applied to the non-group path afterward. Also fixed: `exportGroupParticipants` (frontend store) requests the CSV with `responseType: 'blob'`, so a *failed* request's error body also arrives as a Blob rather than parsed JSON — `e.response.data.message` was always `undefined` and every failure surfaced the same generic fallback text regardless of the real reason; the store action now decodes a Blob error body back into JSON before rethrowing. Still not exercised end-to-end against a real group's `groupMetadata()` participant list — confirm that part next.

**Data retention (2026-07-30)**: `whatsapp_groups` (and its `whatsapp_group_participants`, via FK cascade) pruned past 90 days of inactivity by `app:prune-whatsapp-groups` (daily, see `09-deployment-ops.md`) — keyed off `last_synced_at` when set (falls back to `created_at` for groups that were discovered but never actually exported), so a group that's still being actively re-downloaded isn't pruned just because the row itself is old. Verified live: seeded an old-and-never-synced group, an old-but-recently-synced group, and confirmed only the former (plus its participants, via cascade) got deleted.
- Link Generator (screenshot 53/79) — ✅ built (2026-07-30): pure Laravel, no bridge dependency (`wa.me` links are just URLs). `whatsapp_short_links` (account_id, channel_id, reference_name, phone, message, slug, clicks) + `ShortLinkController` (index scoped to a required `channel_id`, store, destroy) + `WhatsappShortLinkPolicy`. Real click tracking: `GET /wa/{slug}` (`routes/web.php`, public/unauthenticated) → `ShortLinkRedirectController` increments `clicks` and 302-redirects to the plain `wa.me` URL. The shared/copied "Generated Link" (screenshot 79) is `WhatsappShortLink::getShortUrlAttribute()` (`{APP_URL}/wa/{slug}`), not the raw `wa.me` link (`getWaMeUrlAttribute()`) — the raw link only shows as secondary reference text in the saved-links table now. Frontend: `LinkGeneratorPanel.vue` (two-column generate-form + saved-links layout) + `SavedLinksList.vue` (data table, Clicks column now live), added to the WhatsApp feature nav.
- Call Responder (screenshot 52/93/94) — ✅ built (2026-07-30), feasibility check above now resolved by reading the installed fork's own source (`whatsapp-bridge/node_modules/@itsliaaa/baileys/lib/Socket/messages-recv.js`, `Utils/generics.js`): `sock.ev.on('call', ...)` really does fire `{status: offer|accept|reject|timeout|terminate|relaylatency}` and `sock.rejectCall(callId, callFrom)` really does send a reject stanza — confirmed real, not speculative. As expected, there's never live audio/video — this is signaling-only (ring/answer/reject/hangup), matching what the screenshots actually show (a text-auto-reply tool, not a voice bot).
  - Data model: `whatsapp_call_responder_settings` (account_id, channel_id unique, enabled, auto_reject_enabled, reply_delay_seconds, missed_call_reply, after_call_reply, rejected_call_reply, missed_before_answer_reply) — the 4 reply fields map 1:1 onto screenshots 93/94's four textareas. New `whatsapp_call_logs` table (call_id, caller_phone, is_video, status, reply_type, started_at, ended_at) added to back the "Call History" tab, not previously in this doc.
  - Bridge: `sessionManager.js`'s `call` listener skips group calls entirely, forwards every non-group status as a `call.event` webhook plus whether *we* auto-rejected it (`entry.autoRejectedCallIds`, a per-instance ephemeral Set) so Laravel can tell "auto-rejected by us" apart from "declined on the phone itself" without guessing. New `rejectCall(channelId, callId, callFrom)` + `POST /instances/:channelId/calls/:callId/reject`.
  - Laravel: `WebhookController::handleCallEvent` runs a small state machine per `call_id` off the `whatsapp_call_logs` row, since WhatsApp's `terminate` status is generic ("fired when accepted/rejected/timeout/caller hangs up" per the fork's own comment) — `offer` triggers auto-reject (immediate, not delayed) + `missed_call_reply`; `accept` marks the row `answered`; a terminal event then branches on `auto_rejected` flag / whether the row was `answered` / whether the raw status was `timeout` to pick `after_call_reply` / `missed_before_answer_reply` / `rejected_call_reply`. Reuses **`SendAutoReplyJob`** as-is for the actual send (no new job class) — its existing `->delay()` dispatch chain is exactly the "Reply Delay" field. `CallResponderController` (settings/updateSettings/history).
  - Frontend: `CallResponderPanel.vue` (account select w/ connected-status chip, Settings/Call History tabs, master + auto-reject toggles, reply delay, the 4 reply cards with the exact screenshot copy/hints) + `CallHistoryList.vue` (data table).
  - **Fixed after live testing (2026-07-30)**, three real bugs found via real calls:
    1. `handleCallEvent` originally called `BridgeClient::rejectCall()` *synchronously* inside the webhook handler — a blocking call back into the bridge, which itself waits on a Baileys stanza round-trip. Repeatedly blew past the bridge's own 10s webhook-delivery timeout (`[webhook] failed to deliver "call.event": timeout of 10000ms exceeded`). Fixed by moving it to a new `RejectCallJob` (queued, same reasoning as `SendAutoReplyJob`'s own docblock already documents for sends).
    2. `rejectCall()`'s `query(stanza)` call inherits the fork's `defaultQueryTimeoutMs` (60s — confirmed by reading `Socket/socket.js`/`Defaults`), but `BridgeClient::rejectCall()` used the default 15s HTTP timeout — giving up 45s before the bridge could legitimately respond (`cURL error 28: timed out after ~15000ms with 0 bytes received`, on every single attempt). Fixed: bumped to `->timeout(65)`, safe now that it's queued rather than inline.
    3. **The actual "reply never delivered" bug**: `call.from` can arrive as a `@lid` (opaque Linked ID), exactly like inbound messages already can (see `resolveToPhoneJid()`, added earlier for messages) — but the `call` listener used `phoneFromJid(call.from)` directly, skipping that same resolution step. The bogus LID value (confirmed live: a 15-digit non-phone-shaped number) got stored as `caller_phone` and used to address the reply — WhatsApp silently accepts a send to a bogus JID and never delivers it, so `SendAutoReplyJob` reported success with nothing actually arriving. Fixed: the `call` listener now runs `call.from` through `resolveToPhoneJid()` before extracting the phone number for the webhook payload. Separately, also fixed `rejectCall()` itself to use the *original raw* JID (kept in a new per-instance `entry.callJids` map, populated at `offer` time) rather than the resolved phone number Laravel echoes back — the reject stanza needs the real JID format, not a bare digit string.
  - **Still not fully live-verified** after these fixes — confirm a real call again for all three reply-routing branches (auto-rejected, manually declined on the phone, answered-then-hung-up) now that the LID bug is fixed.
  - **Data retention (2026-07-30)**: `whatsapp_call_logs` is pruned past 90 days by `app:prune-whatsapp-call-logs` (daily, see `09-deployment-ops.md`) — covers channels that go idle/disconnected rather than deleted, where the FK cascade never fires. `whatsapp_call_responder_settings` is deliberately **not** pruned by age — it's per-channel configuration, not a log, and stays valid indefinitely as long as the channel exists (cleaned up only via the channel's own `cascadeOnDelete`).
- Public REST API (screenshots 38-50) — ✅ built (2026-07-30), fully functional (not documentation-only): external tools (Zapier/n8n/etc.) can create instances, poll QR/status, register webhooks, and send text/media/interactive/group messages against a real, already-connected account. Auth reuses `channels.access_token` (already existed, auto-generated per channel) + `instance_id` = `channels.id` — no new token table. Instance creation stays UI-only by design: the API's `create_instance`/`reconnect` operate on a channel already created via the normal "Add account" flow, not a from-scratch bootstrap.
  - Routes registered in `routes/api/whatsapp.php` **outside** `auth:sanctum` (own token-based auth instead): `POST /api/whatsapp/{create_instance,set_webhook,reboot,reset_instance,reconnect,send,send_template,send_by_template,send_group}`, `GET /api/whatsapp/{get_qrcode,get_groups}` — `Request::input()` covers both query-string and JSON-body params, matching the reference's own dual-mode examples.
  - New `App\Http\Controllers\Api\Whatsapp\PublicApi\` namespace: `InstanceApiController` (6 methods, thin wrappers around the same `BridgeClient` calls `ChannelController` already makes), `MessageApiController` (`send`/`sendTemplate`/`sendByTemplate` — `sendTemplate` uses this project's own `interactive_config` shape rather than the reference's nested JSON; `sendByTemplate` reuses `WhatsappTemplate::interactiveKind()`/`buildInteractiveConfig()`/`mediaKind()`), `GroupApiController` (`getGroups`/`sendGroup`). Shared traits: `ResolvesPublicApiChannel` (instance_id+access_token → `Channel`, plus the reseller group-gate check) and `SendsWhatsappViaApi` (the actual credit-check → send → decrement logic, shared by `MessageApiController` and `GroupApiController` so it's written once).
  - **Credit enforcement**: every send endpoint (`send`, `send_template`, `send_by_template`, `send_group`) runs through the exact same `WhatsappCreditBalance::forAccount()`/`WhatsappCreditLedger::record()` pair as `MessageController::store()`, `SendCampaignMessageJob`, and `SendAutoReplyJob` — no credits remaining on the account's plan → `402` before the bridge is ever called; disconnected channel → `409`; a confirmed bridge failure → `502` and no credit is spent. An external caller sending through the API costs exactly 1 credit per message, same as sending from the UI, and is blocked outright at 0 balance. Verified live via tinker (402/409/403 paths all exercised directly against real DB rows).
  - **Group API — the only genuinely new bridge capability**: `sessionManager.js`'s `listGroups()` (`sock.groupFetchAllParticipating()`) + bridge route `GET /instances/:channelId/groups`, wrapped by `BridgeClient::listGroups()`. `toJid()` fixed to detect an already-full JID (contains `@`) and pass it through unchanged, instead of unconditionally appending `@s.whatsapp.net` — a group send previously would have been mangled into `{id}@g.us@s.whatsapp.net`.
  - **Reseller gating (new self-service surface)**: `whatsapp_api_settings` (`reseller_account_id` unique FK, `enabled_groups` json) + `WhatsappApiSettings` model, mirroring `ResellerBranding`'s exact `booted()` guard (rejects attachment to a non-reseller account). Resolution is **account-hierarchy based** (`channel.account.parent`), not domain-based like `/api/branding` — API access follows whichever reseller onboarded the client, not the domain they're currently browsing from. No reseller parent (direct platform client, or the account is itself a reseller/super_admin) → unrestricted, so an unaffiliated account is never silently blocked. `WhatsappApiSettingsController` (`show`/`update`, auth:sanctum) — `update` restricted to the reseller's own `owner`-role user, editing only their own row; `show` returns `editable: true/false` so the frontend never needs to know the account's own `account_type`/role directly.
  - **External webhook forwarding**: `channels.external_webhook_url`/`external_webhook_enabled` (new columns) set via `set_webhook`. `WebhookController` forwards `connection.update` and `message.inbound` events to that URL when enabled, HMAC-SHA256 signed (`X-Webhook-Signature`) using the channel's own `access_token` as the secret — queued via new `ForwardExternalWebhookJob` rather than called inline, same reasoning as `RejectCallJob`/`SendAutoReplyJob` (an unreachable/slow third-party URL must never risk blowing past the bridge's own webhook-delivery timeout).
  - Frontend: `ApiPanel.vue` (screenshots 38-50) — account selector, live instance_id/access_token banner, a reseller-only "API Access — Your Client Accounts" card (3 switches, shown only when `api-settings`'s `editable` is true) wired to `WhatsappApiSettingsController`, then the 3 endpoint-group sections (Instance/Messages/Groups) each rendering method badge + real resource URL (query-string or plain+JSON-body, matching the reference's own two-URL pattern for POST-with-body endpoints) + description + params table — all driven by a local endpoint-metadata array substituting the selected channel's real `instance_id`/`access_token`. Sections a reseller has disabled simply don't render for that client. Added to `WhatsappFeatureNav.vue` as "API".
  - **Verified live (2026-07-30)** against the actually-running server + bridge + a real connected account: `get_qrcode` and `get_groups` (returned 80+ real groups from the linked account), `set_webhook` (real DB write, then reverted), auth layer (wrong token → 401 on all 10 write/action endpoints), and validation layer (correct 422s on `send`/`send_template`/`send_by_template`/`send_group`). `create_instance`/`reboot`/`reset_instance`/`reconnect` and an actual successful send were deliberately **not** exercised for real (would disconnect the live session / send a real WhatsApp message) — validated via auth/validation paths only.
  - **Bug found + fixed during live verification**: any `/api/*` route (not just this one — every existing endpoint too) returned an HTML error page (302 redirect for a `ValidationException`, HTML 401/404 page for others) instead of JSON whenever the caller didn't send an explicit `Accept: application/json` header — a dealbreaker for plain curl/simple HTTP clients hitting a REST API. Fixed globally in `bootstrap/app.php` via `$exceptions->shouldRenderJsonWhen(fn ($request, $e) => $request->is('api/*') || $request->expectsJson())` — re-verified the same failing cases now return proper JSON.
- Message History (no dedicated reference screenshot — built to spec from conversation) — ✅ built (2026-07-30): a flat, filterable log of every message (in + out) sent through a given account, distinct from a per-contact chat thread. Pure read path over the already-existing `messages`/`conversations` tables — no new writes, so it has data from every send path in the app (`MessageController`, `WebhookController`'s inbound handler, `SendAutoReplyJob`, `SendCampaignMessageJob`, and the Public API's `SendsWhatsappViaApi`) from day one.
  - `MessageHistoryController::index` — `GET /api/whatsapp/messages/history?channel_id=&search=&direction=&type=&status=&page=`, `auth:sanctum`. Authorizes via `ChannelPolicy@view` (tenant scoping already restricts which channel can even be found — confirmed live: a sibling-tenant user requesting another tenant's `channel_id` gets a `ModelNotFoundException`, not their data). `search` matches message body OR the conversation's `contact_phone`/`contact_name`; real Laravel pagination (`paginate()`), same response shape as `ContactController::index`.
  - Frontend: `MessageHistoryPanel.vue` (account selector + search + direction/type/status filters, 350ms debounced search matching `ContactGroupDetail.vue`'s convention) + `MessageHistoryList.vue` (`v-data-table-server` — true server pagination given message volume can be large, unlike the client-side-fed `v-data-table` used by Call History/Groups). Added to `WhatsappFeatureNav.vue` as "Message History".
  - Verified live via tinker against real data (344 messages across 25 conversations already in the DB from this session's testing): pagination, direction filter (224 inbound), search (149 matches), and the tenant-isolation check above all confirmed against real rows, not fixtures.
  - **Data retention (2026-07-30)**: `messages` pruned past 90 days by `app:prune-whatsapp-messages` (daily, see `09-deployment-ops.md`), keyed off `created_at`. Parent `conversations` rows are **not** pruned — they're lightweight contact/lead metadata, not log data, so the CRM/lead linkage survives even once a conversation's old messages are gone. Verified live: seeded a 95-day-old and a 10-day-old message inside a rolled-back DB transaction, confirmed only the 95-day-old one was deleted, real data untouched.

### 1d — Bot Builder (last, deliberately scoped down from the reference app)
- Reuse the `flow_definition` json pattern already planned for `ivr_flows` (voice) — same visual-builder concept, different node vocabulary
- v1 node set only: Start, Text, Text Input (name/number/email/phone), Condition, Webhook (outbound HTTP), End — enough for real linear/branching flows
- Defer the long tail from screenshots 60-62 (Zapier, Make.com, Pabbly, Chatwoot, OpenAI, Anthropic, Mistral, ElevenLabs, NocoDB, Segment, Groq, Zendesk, Posthog, Perplexity, DeepSeek, WooCommerce, etc. — 25+ integrations) to build **on demand**, not upfront. Ship the flow engine + Webhook node first; a webhook node can reach most of those services without a dedicated integration.
- Marketplace/template library (screenshot 57) and JSON import/export (screenshot 58) are cheap once flow_definition exists — low priority but low cost, can ride along with 1d
- **Do not build all 25+ integration nodes as part of "Phase 1" under any interpretation** — this alone is a multi-month effort in the reference app; treat it as its own future phase once the flow engine is proven with the Webhook escape hatch.

## Frontend
Follow `10-frontend-conventions.md` exactly: `views/whatsapp/` with one `WhatsappView.vue` shell + per-feature sub-views/components (dashboard, connect, send, bulk, autoresponder, chatbot, templates, api, groups, call-responder, forms), `stores/whatsapp/`, `router/routes/whatsapp.js`. Sidebar entry list matches the reference app's feature list (feature-inventory line 12), including Form Builder — see below.

**Note on Form Builder (screenshot 51/91/92):** ✅ built (2026-07-30) — **explicitly overriding** the earlier recommendation on this line (kept below for the reasoning trail): it's genuinely not WhatsApp-specific, but the user decided to build it nested under the WhatsApp module anyway, matching the reference app's own structure, rather than as a standalone module.
- Data model: `whatsapp_forms` (account_id, name, slug unique, status draft/active, `fields` json — ordered array of `{id, type, label, placeholder, required, options}`, `automation_config` json, `submissions_count`, `revenue` decimal) + `whatsapp_form_submissions` (form_id, lead_id nullable, `data` json). `WhatsappFormSubmission` deliberately has **no** `BelongsToTenant` — public visitors have no authenticated session to scope against, so `account_id` is stamped explicitly from the parent form everywhere it's touched (same reasoning as `MediaFile` uploads elsewhere).
- No screenshot shows the actual public-facing filled form, the Automations panel's real contents, or how revenue is computed — three deliberate scope decisions: (1) built a minimal public fill-out page anyway, since without one the feature could never be tested end-to-end (dashboard stats would stay 0 forever); (2) scoped Automations to two concrete toggles — auto-create a CRM lead (reuses `LeadController::store`'s exact pipeline/stage-resolution pattern, name/phone/email picked from the first text/whatsapp/email-type field in the form's own schema) and auto-send a WhatsApp confirmation (reuses `SendAutoReplyJob` as-is, same as every other auto-reply feature in this doc) — rather than a speculative visual rule engine; (3) `revenue` is a real column/aggregate that legitimately stays 0 — no payment/commerce field type exists in the visible palette and no Commerce module exists yet (Phase 3), so nothing ever writes to it, same precedent as the CRM dashboard's "Unread" stat.
- Backend: `FormController` (authenticated CRUD + `dashboard()`) under the existing `auth:sanctum` group; `FormPublicController` (`show`/`submit`) registered *outside* it, same pattern as `/webhook`. File-upload fields reuse the existing shared media library (`App\Services\Media\MediaStorage` — added a `form_uploads` purpose alongside `whatsapp_media`/`exports`) rather than inventing separate storage handling, including the same per-account plan storage-limit enforcement `MediaFileController::store` already does.
- Frontend: `FormsModulePanel.vue` (list/builder view-switch, same pattern as `ChatbotPanel.vue`) → `FormsPanel.vue` (dashboard, screenshot 91) + `FormBuilderPanel.vue` (screenshot 92 — palette is click-to-add per its own "Click elements on the left to start" copy, canvas reordering uses **`vuedraggable`**, already a project dependency used identically for the CRM Kanban board). Public side is a standalone route (`/f/:slug` → `PublicFormView.vue`), a sibling to the auth routes rather than nested under the authenticated app shell — dynamically renders all 12 field types with real client-side validation (email/phone/required) and a working file input.
- Verified via tinker end-to-end (not just unit-level): created a form with a representative field mix, submitted through `FormPublicController::submit` directly, confirmed the `WhatsappFormSubmission` + auto-created `Lead` came out correctly linked with the right heuristic-derived name/phone/email, confirmed validation correctly rejects a missing required field + invalid dropdown value, confirmed a `draft` form correctly 404s on the public route. **Not yet verified in an actual browser** (drag-and-drop reordering, the public page's real rendering/upload flow) — no dev server run, per standing preference; flagging this the same way other frontend-only gaps have been flagged all session.

**Round 2 (2026-07-30) — real usage surfaced a functional bug and three more reference screenshots (95-102) drove a substantial expansion:**
1. **Fixed: public submissions were silently failing.** `bootstrap/app.php` applies `statefulApi()` globally to the whole `api` middleware group, so Sanctum treats any same-origin POST — including `/forms/{slug}/submit`, deliberately outside `auth:sanctum` — as needing CSRF protection. `PublicFormView.vue` never fetched the CSRF cookie (only the login flow does, via `ensureCsrfCookie()`), so every real submission 419'd. Fixed by calling `ensureCsrfCookie()` on mount, same as login.
2. **Added a "Time" field type** (`date_format:H:i` validation) alongside the original 12.
3. **Configurable success page**: `whatsapp_forms.success_message`, editable in the config dialog, shown on the public thank-you card in place of the generic fallback.
4. **Forms list rebuilt as a card grid** (screenshot 95, was a data table): `FormsPanel.vue` now shows a LIVE/DRAFT chip, public URL preview, and Stats/Share/Leads/View actions per form; Edit/Delete moved into a kebab menu. "Stats" opens a lightweight dialog (submissions count/status/created date/field count) — no dedicated screenshot existed for its contents.
5. **New leads list** (screenshot 96): `FormLeadsPanel.vue`, reached via a form card's "Leads" button — `FormController::submissions()` (lead relation eager-loaded for the "CRM Lead" badge) + `FormController::exportSubmissions()`. "Export PDF" in the reference is built as **CSV** instead (explicit decision, confirmed with the user) — same pattern every other export in this app already uses (`ContactController`, `GroupController`) rather than adding this project's first PDF dependency for one button. Visitor insight (`ip_address`/`user_agent`, captured via `$request->ip()`/`$request->userAgent()`) shown per submission.
6. **"Public Form Link" panel** (screenshot 97) added to the bottom of the builder canvas once a form is saved — URL + copy button, Open Form, Get Embed Code (copies an `<iframe>` snippet).
7. **Automations rebuilt into the full "Global Form Configuration" dialog** (screenshots 98-102, 5 tabs), replacing the earlier 2-toggle version:
   - **General** — editable form slug (`FormController::resolveSlug()`: sanitizes, auto-suffixes `-2`/`-3`/... on collision, scoped correctly on update via `excludeId` so a form doesn't collide with itself), WhatsApp account (`whatsapp_forms.channel_id`, new top-level column — shared by both WhatsApp automations below), Success Action (`message` or `redirect` — `whatsapp_forms.success_action`/`success_redirect_url`; `PublicFormView.vue` navigates via `window.location.href` instead of showing the thank-you card when set to redirect), Enable reCAPTCHA (stored, **not verified** — no reCAPTCHA site/secret keys exist anywhere in this project's config).
   - **WhatsApp** — Admin Notification (real: sends to `admin_notify_phone` via the form's `channel_id`) and User Auto-Reply (real: replaces the old single confirmation toggle, replies to the submitter's own WhatsApp-type field value). Both support `{name}`/`{data}` template placeholders (`FormPublicController::renderTemplate()` — `{name}` = first text-field value, `{data}` = a plain-text summary of every submitted field, matching screenshot 99's "New form submission: {data}" example exactly). Both reuse `SendAutoReplyJob` as-is.
   - **CRM & AI** — "Create Lead: Instant/Disabled" is real (renamed from the old boolean toggle, same underlying behavior, always uses the account's default pipeline — the tab has no pipeline picker). "Assign To" (round-robin) and "AI Qualification" are **deliberately inert** — presented as locked dropdowns with an explanatory hint rather than removed, since no lead-assignment or AI-scoring system exists in this codebase yet.
   - **Payment**, **IVR Call** — both **deliberately inert** (locked dropdowns, "No"/"No Call" only) — no Commerce/payment module and no IVR/voice-calling system exist yet. The IVR tab was explicitly called out by the user as "blank for now" rather than half-building a voice feature; Payment got the same treatment for consistency with the existing Revenue-stub precedent.
- Re-verified end-to-end via tinker after all of the above: submitted through the real controller with a time field (validated `14:30`), confirmed `ip_address`/`user_agent` landed on the submission, confirmed the CSV export's header/data rows match the form's actual field labels, confirmed a custom slug sanitizes correctly (`"my custom slug!!"` → `my-custom-slug`) and collision-suffixes correctly (`-2`) both on create and update, confirmed `success_action`/`success_redirect_url` round-trip through the public `show()` endpoint, confirmed Admin Notification and User Auto-Reply both correctly fire when the form's channel is connected and correctly no-op when it isn't (tested by temporarily flipping a real channel's status in the DB and reverting after).

**Round 3 (2026-07-30) — real submission attempt surfaced an R2 config bug and a UX gap, not a logic bug:**
- **R2 upload bucket mismatch**: `config/filesystems.php`'s `form_uploads` purpose referenced `R2_BUCKET_FORM_UPLOADS`/`R2_URL_FORM_UPLOADS` (plural) but the actual `.env` has them singular (`R2_BUCKET_FORM_UPLOAD`/`R2_URL_FORM_UPLOAD`) — `env()` silently returned null for the misnamed vars, so every file-field submission's R2 upload failed. Fixed to match the `.env` names that were actually set; verified live — pushed a real object to the `form_uploads` R2 bucket, confirmed it exists, deleted it.
- **"User Auto-Reply won't enable" was working as designed, just poorly explained**: the account's real "Lead Form" test row has no `whatsapp`-type field in its schema and no `channel_id` set (it predates this round's rebuild), so the toggle was correctly disabled — auto-replying needs a phone number to reply to, and sending anything needs a WhatsApp account selected. The dependency wasn't visible enough: replaced the small caption hint with a proper `v-alert`, and added a second alert at the top of the WhatsApp tab when no account is selected under General (since that silently blocks *both* Admin Notification and User Auto-Reply, not just the latter). Add a "WhatsApp Number" field and pick an account under General to actually use these on that form.

## Open decisions
1. ~~Node bridge service hosting~~ — **decided**: own process, colocated on the same box initially (see 1a note above).
2. ~~Credit ledger vs. simple counter~~ — **decided**: ledger + dedicated balance table (see data model note above).
3. Call Responder feasibility with Baileys — spike this early since it gates whether 1c includes it at all.
4. Whether the public REST API (1c) should be versioned alongside the future official "API & Automation" v2 module (`01-feature-inventory.md` line 111) or kept fully separate per-channel — recommend separate for now (different auth model: per-instance access_token vs. account-level API tokens), revisit when the general API & Automation module is actually scoped.
